for (let i = 1; i < 11; i++) { // I made a loop ranging and starts to 1 and stops when it reach 11 (1-10)
    if (i % 2 === 0) { // the modulo check for the remainder that is divided by 2 to check if it was equals to zero, if that was the case then the current value of i is even and is then output on the console
        console.log(i);
    }
}